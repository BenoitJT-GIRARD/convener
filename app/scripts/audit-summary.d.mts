/* Types for audit-summary.mjs, so the test suite can import it typed. The
 * script itself stays plain ESM -- see handbook-files.d.mts for why. */
export declare function auditSummary(audit: unknown): string;

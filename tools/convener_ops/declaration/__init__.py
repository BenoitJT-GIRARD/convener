"""What one instance declares about itself: the boundary between what the product
keeps and what it hands over, the published identity, the paths both are written in,
the needles a sweep looks for, the integrations a declaration switches on, and the
loader that reads the files all of it lives in."""

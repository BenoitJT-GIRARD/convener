"""How the board decides and what records the decision: the voting rule, the grammar
of the commits that carry it, the register derived from them, the validator that
refuses a malformed record, and what reaches a volunteer who is not looking at the
app."""

"""The scheduled upkeep of a repository nobody is watching: the state sweep, the
Actions budget, and the evidence that the retention run, the queue drain and the
registration routing are each still happening at all."""

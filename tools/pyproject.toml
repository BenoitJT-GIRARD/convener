[project]
name = "convener-ops"
# The product's version, and the only place in this repository it is
# written. This package is never published to an index -- nothing installs
# it but this repository's own `uv sync` -- so the field was free, and
# `CHANGELOG.md`'s newest entry, the annotated tag and this line all name
# one state because there is one number for them to name.
# `tools/scripts/generate_changelog.py --check` reads this file and fails a
# changelog whose newest entry says something else, which is what stops a
# release bumping one of the two.
#
# The interface the number is about is the merge a duplicate does, and
# nothing else: `declarations/boundary.yml` declares which paths that
# duplicate owns, so a major says one of them changed shape under it and a
# minor says a merge is the whole of what a release takes.
# `CHANGELOG.md`'s own *What a version names* states that for the person
# reading a release, and its first entry carries the argument for starting
# at 1.0.0 rather than at a 0.x.
version = "1.0.0"
description = "Operational tooling for a seminar series run from its own repository"
requires-python = ">=3.12"
dependencies = [
  "pyyaml>=6.0.2",
  # zoneinfo has no bundled tz database; Windows (and minimal Linux images)
  # have none on disk, so this ships one. Required for sweep.py's DST handling.
  "tzdata>=2025.1; sys_platform == 'win32'",
  # No asymmetric primitive exists in the standard library (hashlib, hmac and
  # secrets are hashing and symmetric only), and eventkeys.py's whole design
  # depends on one: the browser only ever holds the public half, which is
  # only true if a public key cannot decrypt -- a property no symmetric
  # scheme has. The choice was between this well-audited library and
  # hand-rolling RSA in the one module that handles other people's personal
  # data; see tools/convener_ops/journey/eventkeys.py for the rest of that reasoning.
  "cryptography>=43",
  # A certificate carries a machine-readable code, and the delivery step's
  # own ruling requires the library that draws it to be pure Python, no
  # binary wheel, no Pillow -- verified before adding it, not assumed: segno
  # ships one universal wheel (py3-none-any, checked directly against the
  # built artefact, not only the classifier), has zero required dependencies
  # for this project's Python floor (its one dependency,
  # importlib-metadata, is conditional on python_version < '3.10'; this
  # project requires >=3.12), is BSD-3-Clause (the licence file itself, not
  # only the PyPI classifier), and renders SVG natively through
  # `svg_inline()` -- an embeddable `<svg>...</svg>` fragment, no Pillow, no
  # raster step at all. See tools/convener_ops/journey/delivery.py for where it is used
  # and why HTML+SVG rather than a raster format.
  "segno>=1.6",
]

[project.scripts]
convener-validate = "convener_ops.cli:validate"
convener-check-config = "convener_ops.cli:check_config"
convener-sweep = "convener_ops.cli:sweep"
convener-public-data = "convener_ops.cli:public_data"
convener-survey-status-public-data = "convener_ops.cli:survey_status_public_data"
convener-registration-routing-public-data = "convener_ops.cli:registration_routing_public_data"
convener-handle-proposal = "convener_ops.cli:handle_proposal"
convener-check-commits = "convener_ops.governance.commit_format:check_commits"
convener-commit-range = "convener_ops.governance.commit_format:commit_range"
convener-register = "convener_ops.cli:register"
convener-notify-immediate = "convener_ops.cli:notify_immediate"
convener-notify-digest = "convener_ops.cli:notify_digest"
convener-alert-secret-workflow-run = "convener_ops.cli:alert_secret_workflow_run"
convener-registration-secret-name = "convener_ops.cli:resolve_registration_secret"
convener-handle-registration = "convener_ops.cli:handle_registration"
convener-send-confirmation = "convener_ops.cli:send_confirmation"
convener-resend-confirmation = "convener_ops.cli:resend_confirmation"
convener-encrypt-identifier = "convener_ops.cli:encrypt_identifier"
convener-encrypt-attendance-export = "convener_ops.cli:encrypt_attendance_export"
convener-match-attendance = "convener_ops.cli:match_attendance"
convener-release-recording = "convener_ops.cli:release_recording"
convener-discard-recording = "convener_ops.cli:discard_recording"
convener-issue-certificates = "convener_ops.cli:issue_certificates"
convener-certificates-public-data = "convener_ops.cli:certificates_public_data"
convener-reissue-certificate = "convener_ops.cli:reissue_certificate"
convener-revoke-certificate = "convener_ops.cli:revoke_certificate"
convener-deliver-certificates = "convener_ops.cli:deliver_certificates"
convener-deliver-certificate = "convener_ops.cli:deliver_certificate"
convener-retention-sweep = "convener_ops.cli:retention_sweep"
convener-record-destructions = "convener_ops.cli:record_destructions"
convener-record-retention-run = "convener_ops.cli:record_retention_run"
convener-check-retention-liveness = "convener_ops.cli:check_retention_liveness"
convener-actions-usage-window = "convener_ops.cli:actions_usage_window"
convener-record-actions-usage = "convener_ops.cli:record_actions_usage"
convener-check-actions-usage-liveness = "convener_ops.cli:check_actions_usage_liveness"
convener-erase-registration = "convener_ops.cli:erase_registration"
convener-plan-queue-drain = "convener_ops.cli:plan_queue_drain"
convener-drain-queue = "convener_ops.cli:drain_queue"
convener-confirm-queued-registrations = "convener_ops.cli:confirm_queued_registrations"
convener-record-queue-watch = "convener_ops.cli:record_queue_watch"
convener-check-queue-liveness = "convener_ops.cli:check_queue_liveness"
convener-check-credential-expiry = "convener_ops.cli:check_credential_expiry"
convener-check-registration-routing = "convener_ops.cli:check_registration_routing"
convener-invite-survey = "convener_ops.cli:invite_survey"
convener-record-survey-invitation = "convener_ops.cli:record_survey_invitation"
convener-render-visual-fixtures = "convener_ops.cli:render_visual_fixtures"
convener-render-template-fixtures = "convener_ops.cli:render_template_fixtures"
convener-render-poster-fixtures = "convener_ops.cli:render_poster_fixtures"
convener-render-visuals = "convener_ops.cli:render_visuals"
convener-render-announcements = "convener_ops.cli:render_announcements"
convener-agenda-internal = "convener_ops.cli:agenda_internal"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["convener_ops"]

[dependency-groups]
dev = [
  "pytest>=8.3",
  "pytest-cov>=6.0",
  "ruff>=0.9",
  "mypy>=1.14",
  # Kept beside ruff rather than folded into it, on a measurement rather
  # than on the assumption that a port covers its original: ruff's own `S`
  # rules see neither `B404` nor a `subprocess.run` whose argv is entirely
  # literal. The comparison, finding by finding, is in `[tool.ruff.lint]`'s
  # comment below, and `tools/tests/repository/test_workflows.py::test_the_security_
  # scanner_is_declared_run_and_reads_its_own_suppressions` is what stops
  # this line and `quality.yml`'s own step from being removed one without
  # the other.
  "bandit>=1.8",
  "pip-audit>=2.7",
  "types-pyyaml>=6.0.12",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
# `S` (flake8-bandit) is absent, and that is a measured decision rather
# than an omission. Ruff's `S` rules are a port of bandit, so enabling
# them would remove a tool, a dependency and a `quality.yml` step -- which
# is worth doing only if the port sees what the original sees. It does
# not. Both were run over `convener_ops` (bandit 1.9.4, ruff 0.16.3):
#
#   bandit -q -r convener_ops        0 findings
#   bandit --ignore-nosec           31  B603x14 B607x11 B404x3 B310x2 B506x1
#   ruff --select S                 32  S607x11 S603x10 S105x6 S310x4 S506x1
#
# The zero is what twenty `# nosec` comments in `convener_ops` account
# for; the second line is the set the two tools have to be compared on.
# Every count here is re-measured rather than carried forward: the
# previous set said eighteen, twenty-seven and thirty, and each had gone
# stale as the package grew subprocess call sites. The shape of the
# argument did not move; the arithmetic under it did, twice, unnoticed.
#
# Seven of those thirty-one have no equivalent under `select = ["S"]`,
# and that is what settles it. `B404` (`import subprocess`, at three call
# sites) is `S404` in ruff, and
# `S404` is a preview rule: selecting it prints "has no effect because
# preview is not enabled", and nothing in this project runs ruff in
# preview. `B603` fires on every `subprocess.run` -- fourteen; `S603`
# deliberately does not fire when every element of the argv is a string
# literal, which is exactly the four it passes over, all four of them a
# `git` invocation with nothing interpolated into it. Ruff is the
# better-aimed rule on those four and bandit is the only one that sees
# the import at all, so neither tool contains the other.
#
# Ruff sees eight findings bandit misses, in two kinds, and neither pays
# for the swap. `S310` also flags `urllib.request.Request` (bandit's
# `B310` blacklist names `urlopen` and `urlretrieve` only), which is two
# more findings on `platform_fcc.py`'s existing pair. `S105` flags six
# `Final`-annotated constants -- `_TOKEN`, `_PASSWORD_ENV`, `TOKEN`,
# `SECRET`, `TOKEN_ENV`, `SECRET_NAME` -- that hold a regex fragment, an
# environment variable's *name* or a category label, never a password;
# bandit's `B105` reads `Assign` and not `AnnAssign`, so it never sees
# them at all.
#
# The swap would also cost more than the step it removes. `# nosec` is
# bandit's syntax and ruff reads `# noqa`, so all twenty suppressions
# would have to be rewritten and eight more added for the false positives
# above. And `select` applies to everything CI lints (`ruff check .`
# from `tools/`), not only to the package bandit is pointed at: `S` finds
# 6,302 there, 6,171 of them `S101` -- `assert`, in this project's own
# tests -- so it would need a `per-file-ignores` block bandit never
# needed.
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
# `scripts` sits beside the package rather than inside it -- the wheel
# ships `convener_ops` alone -- and is imported by its tests. Named here
# rather than reached through `mypy_path`, so that the same list states
# what is checked and where the modules are found.
files = ["convener_ops", "tests", "scripts"]

[tool.coverage.run]
# `branch` belongs here, not under [tool.coverage.report] -- coverage.py
# only recognises it as a [run] option (a misplaced copy is silently
# ignored, with a warning, not an error). Verified by running the suite
# with and without this section and comparing the Branch/BrPart columns.
branch = true

[tool.coverage.report]
# 90, and the suite measures far above it: 96.43% on this head, 10,792 of
# 11,192 measured statements and branch exits covered. The floor is a
# backstop and the distance between the two is what it costs.
#
# The distance is worth stating in the unit the gate is actually in.
# coverage.py's percentage is statements *plus* branch exits together --
# 8,590 and 2,602 of them here -- so a slack computed against the
# statement count alone under-reads it by the whole branch denominator.
# Measured against the real total: 90% of 11,192 is 10,073, and 10,792 are
# covered today, so 719 currently-covered statements or branch exits could
# go uncovered before the gate would fail. That is gross regression, a
# chunk of untested code landing at once, and nothing finer than that. A
# test that exercises a line without asserting anything meaningful about it
# satisfies this gate exactly as well as one that doesn't exist here at
# all. Compare app/vite.config.ts, which enforces 90/90/85/90 while the app
# measures 98.48/94.47/99.56/99.09 -- same shape of gap, same limits to
# what a floor this far below the truth can catch.
#
# The real guard against a weak test is mutation testing -- breaking the
# line or branch a test claims to cover and checking the test then fails --
# and a reviewer who spot-checks that a new test actually bites, not this
# number. fail_under is a backstop against a large regression, never a
# claim that passing it means the suite is any good. (Every figure above is
# measured. Recompute them rather than adjusting the words around them,
# because the arithmetic is the whole of what they say.)
fail_under = 90
show_missing = true

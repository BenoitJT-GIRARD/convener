{
  "name": "convener-site",
  "private": true,
  "scripts": {
    "build": "eleventy --output=_site",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}

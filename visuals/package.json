{
  "name": "convener-visuals",
  "private": true,
  "description": "Task 5 (phase 6): the pinned render step and its image comparison -- puppeteer (full, bundled Chromium) is isolated to this package on purpose, so only this one CI job pays for the Chrome-for-Testing download; see .github/workflows/visuals.yml and render-and-compare.mjs's own module comment for the full reasoning (P-2).",
  "scripts": {
    "check": "node render-and-compare.mjs",
    "update-references": "node render-and-compare.mjs --update"
  },
  "devDependencies": {
    "puppeteer": "25.8.0"
  }
}

name: Close gate issues

on:
  push:
    branches: [main]
    paths:
      - 'data/speakers.yml'
      - 'data/events.yml'
      - '.github/scripts/close_gate_issue.py'

permissions:
  issues: write
  contents: read

jobs:
  close:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: pip install pyyaml
      - run: python .github/scripts/close_gate_issue.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
